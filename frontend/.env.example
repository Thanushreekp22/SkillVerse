# Backend API base URL for production builds.
# Leave unset for local development (Vite proxies /api -> http://127.0.0.1:5001).
# Set this in Vercel project settings before building:
VITE_API_URL=https://your-backend-service.onrender.com/api